#!/bin/bash

# Add any logic that is common to both the peer and docker environments here
echo "apt-get update -qq"
#apt-get update -qq

# Used by CHAINTOOL
#echo "apt-get install -y default-jre"
#apt-get install -y default-jre
